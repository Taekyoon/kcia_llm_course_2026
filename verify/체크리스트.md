# VESSL 실행 검증 체크리스트

> 대상 환경: `cloud.vessl.ai/KCIA/Coupang/workspaces/wsp-ydnpxjpkyl19`
> Claude는 이 환경에 접근할 수 없습니다. **직접 실행하신 뒤 결과를 회신**해 주세요.
> 결과는 `verify/결과.md` 에 기록됩니다.

## 진행 순서

| 단계 | 할 일 | 예상 시간 |
|---|---|---|
| ① | `verify/00_환경점검.py` 실행 → 출력 전체 회신 | 1분 |
| ② | (①회신 후) `verify/01_스택설치.py` 실행 → 출력 전체 회신 | 10~20분 |
| ③ | 아래 노트북별 점검 — **여유가 되는 만큼만**. 우선순위 순으로 정렬돼 있습니다. | 노트북당 5~30분 |

**②까지만 해주셔도 리포트의 상당 부분이 확정됩니다.** ③은 남는 불확실성을 줄이는 단계입니다.

> ⚠️ vLLM 서버를 띄우는 노트북(퓨샷 · Amazon 요약 · BM25 RAG)은 **GPU를 통째로 점유**합니다.
> 동시에 두 개를 띄우지 마시고, 하나 끝나면 커널을 재시작한 뒤 다음으로 넘어가세요.

---

## 기록 방법

각 항목에 아래 중 하나로 표시해 주세요. **실패 시 에러 메시지 원문을 그대로** 붙여주시면 됩니다 (요약하지 마세요 — 원문이 판정 근거가 됩니다).

- `OK`
- `실패` + 에러 원문
- `건너뜀`

---

## ③-1. 🔴 NER 실습 — 최우선

`notebook/1일차/HPC_NER실습` · 22셀

**지금 당장 실패할 것으로 예상됩니다.** `kor_ner` 데이터셋이 HuggingFace에서 404입니다.

| # | 확인 항목 | 위치 | 결과 |
|---|---|---|---|
| 1 | `load_dataset("kor_ner")` 가 실패하는가, 어떤 에러인가 | 셀 8 | |
| 2 | 대체안 `load_dataset("klue/klue", "ner")` 는 로드되는가 | — | |
| 3 | klue/klue ner 의 컬럼명과 라벨 목록은? (`kor_ner` 는 `ner_tags`/`tokens` 사용) | — | |
| 4 | 마지막 셀의 `./ner_seq_classifier/checkpoint-183` 하드코딩 경로가 실제 학습 결과와 맞는가 | 셀 22 | |

> ①②를 하셨다면 1~3은 `01_스택설치.py` 출력에 이미 들어 있습니다. 중복 실행 불필요.

## ③-2. 🔴 퓨샷 실습

`notebook/2일차/HPC_퓨샷실습` · 64셀

| # | 확인 항목 | 위치 | 결과 |
|---|---|---|---|
| 1 | `python -m vllm.entrypoints.openai.api_server` 로 서버가 뜨는가 (deprecation 경고 여부) | 셀 6 | |
| 2 | `vllm serve LGAI-EXAONE/EXAONE-3.5-2.4B-Instruct` 로는 뜨는가 | — | |
| 3 | **EXAONE-3.5 가 vLLM 0.27 에서 로딩되는가** (지원표에 3.0/4.0만 등재) | — | |
| 4 | `extra_body={"guided_choice": [...]}` 가 동작하는가 | 셀 23·26·43 | |
| 5 | `extra_body={"guided_json": ...}` 가 동작하는가 | 셀 59·63 | |
| 6 | 대체안 `extra_body={"structured_outputs": {"choice": [...]}}` 는 동작하는가 | — | |
| 7 | `load_dataset("e9t/nsmc")` 실패 여부 | 셀 11 | |

## ③-3. 🔴 Amazon 요약 실습

`notebook/3일차/HPC_Amazon요약실습` · 55셀

**7개 Step 전부가 `guided_json` 에 의존**합니다. ③-2의 4~6이 확인되면 이 노트북의 운명도 결정됩니다.

| # | 확인 항목 | 위치 | 결과 |
|---|---|---|---|
| 1 | `guided_json` 사용 6곳이 전부 같은 방식으로 실패하는가 | 셀 12·16·20·26·35·42 | |
| 2 | `load_dataset('Taekyoon/test_amazon')` 로드되는가 | 셀 5 | |
| 3 | 마지막 파이프라인 셀(`data.map(...)`)이 완주하는가 | 셀 50 | |

## ③-4. 🔴 BM25 RAG 실습

`notebook/3일차/HPC_BM25_RAG실습` · 31셀

| # | 확인 항목 | 위치 | 결과 |
|---|---|---|---|
| 1 | `llama-index-llms-vllm` 패키지가 아직 설치·동작하는가 | 셀 3 | |
| 2 | `Settings.llm = Vllm(model='LGAI-EXAONE/...')` 가 뜨는가 | 셀 4 | |
| 3 | `load_dataset('heegyu/kowikitext', trust_remote_code=True, ...)` — `trust_remote_code` 인자가 제거돼 실패하는가 | 셀 7 | |
| 4 | 인자를 빼면(`trust_remote_code` 제거) 로드되는가 | — | |
| 5 | **`Settings.embed_model` 을 지정하지 않아도 인덱싱이 되는가** (BM25는 어휘 기반이라 불필요할 수 있음 — 확인 필요) | 셀 13 | |
| 6 | ⚠️ **핵심**: 셀 21에서 프롬프트를 고친 뒤(`prompts[...].default_template.template = ...`) 셀 23의 답변이 **실제로 한국어로 바뀌는가?** <br>`get_prompts()` 가 사본을 반환하므로 **바뀌지 않을 것으로 예상**됩니다. 셀 22는 바뀐 것처럼 출력되지만 엔진에는 반영이 안 됩니다. | 셀 21·22·23 | |
| 7 | `update_prompts({...: PromptTemplate(...)})` 로 바꾸면 반영되는가 | — | |

## ③-5. 🟡 SFT / DPO / GRPO 실습

`notebook/2일차/HPC_SFT 실습`(28셀) · `HPC_DPO 실습`(23셀) · `HPC_GRPO 실습`(26셀)

셋이 구조가 거의 같아서 **SFT 하나만 해보시면 나머지는 유추 가능**합니다.

| # | 확인 항목 | 위치 | 결과 |
|---|---|---|---|
| 1 | `SFTConfig(max_seq_length=...)` 가 `TypeError` 를 내는가 (→ `max_length` 로 개명) | SFT 셀 20 | |
| 2 | `get_peft_model()` 로 먼저 감싼 모델을 `SFTTrainer` 에 넘겨도 동작하는가 | SFT 셀 18·20 | |
| 3 | `trainer.tokenizer.save_pretrained(...)` 가 `AttributeError` 를 내는가 | SFT 셀 23 / DPO 셀 18 / GRPO 셀 21 | |
| 4 | `trainer.processing_class.save_pretrained(...)` 로는 되는가 | — | |
| 5 | GRPO: `GRPOConfig` 의 `vllm_mode` 기본값이 `colocate` 로 바뀐 게 학습에 영향을 주는가 | GRPO 셀 18 | |
| 6 | 학습이 1스텝이라도 도는가 (`max_steps=1` 로 낮춰서 확인) | | |

> 시간 절약: `num_train_epochs` 대신 `max_steps=1` 로 바꿔 실행하시면 됩니다.
> SFT 셀 20 `num_train_epochs=3` → `max_steps=1`, DPO 셀 15 `num_train_epochs=2` → `max_steps=1`

## ③-6. 🟡 Classification 실습

`notebook/1일차/HPC_Classification실습` · 21셀

| # | 확인 항목 | 위치 | 결과 |
|---|---|---|---|
| 1 | `load_dataset("nsmc", trust_remote_code=True)` 실패 (인자 제거 + 스크립트 데이터셋) | 셀 8 | |
| 2 | 대체안 `load_dataset("e9t/nsmc", revision="refs/convert/parquet")` 로드되는가 | — | |
| 3 | `Trainer(..., tokenizer=tokenizer)` 가 `TypeError` 를 내는가 (→ `processing_class`) | 셀 19 | |
| 4 | `evaluate.load("accuracy")` 가 datasets 5.x 와 함께 동작하는가 | 셀 14 | |
| 5 | 마지막 셀 `./nsmc_classifier/checkpoint-938` 하드코딩 경로가 실제와 맞는가 | 셀 20 | |

## ③-7. 🟢 MiniGPT 실습 — 낮은 우선순위

`notebook/1일차/HPC_MiniGPT실습` · 81셀

웹 조사 결과 **keras-hub API는 전부 그대로 유효**합니다. 실패 가능성이 낮습니다.

| # | 확인 항목 | 위치 | 결과 |
|---|---|---|---|
| 1 | `import keras_hub` 가 되는가, 버전은? | 셀 4 | |
| 2 | `keras_hub.tokenizers.compute_word_piece_vocabulary(...)` 동작 (tensorflow-text 필요) | 셀 29 | |
| 3 | 샘플러 호출 `sampler(next=fn, prompt=..., index=...)` 가 현 시그니처와 맞는가 | 셀 68·71·73·76·78 | |
| 4 | `model.fit(...)` 1 epoch 완주하는가 | 셀 61 | |

---

## 회신해 주실 것

1. `00_환경점검.py` 출력 **전체**
2. `01_스택설치.py` 출력 **전체** (마지막 JSON 포함)
3. 위 표에서 진행하신 항목의 결과 (실패는 **에러 원문 그대로**)

이 세 가지가 `review/02_문제점.md` 의 재현성 항목 근거가 됩니다.
회신되지 않은 항목은 리포트에 **"미검증"** 으로 명시하고, 추측으로 채우지 않습니다.
